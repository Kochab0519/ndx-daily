# ndx-daily — AGENTS.md

## 项目性质

静态 GitHub Pages 站点，部署于 `kochab.ccwu.cc`。每日发布纳指 100 动态简报。

## 工作流

- 直接编辑 `index.html` — 无构建步骤，无框架，无包管理器
- 提交到 `main` 分支 → GitHub Pages 自动部署
- 自定义域名通过 `CNAME` 文件配置（必须保留在仓库根目录）

## 约定

- 内容为中文金融简报（纳指 100 动态简报）
- CSS 内联在 `<style>` 中 — 无外部样式表
- 所有数据均为手写/静态 — 无 JS 数据请求
- 日期敏感：每次发布新简报时需更新 HTML 中的日期字符串和数据值
